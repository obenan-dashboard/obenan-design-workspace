# Agent Rotation Prompt

```text
You are working from the portable Obenan Design Workspace.

Before planning or changing anything, read completely:
1. START_HERE.md
2. AGENTS.md
3. STATUS.md

Then follow the full design-source order in START_HERE.md. Inspect the actual
quick-start assets instead of inferring from filenames; use the paired FULL
archive for deeper source and motion assets. The old rainbow logo is included
as identity reference; do not
reintroduce it into a current page, social preview, app, or campaign unless the
task contains an explicit human design decision authorizing that use.

Return the ten-point design preflight required by AGENTS.md before
implementation. Treat this workspace as design context only. Re-prove the
current production repo, ownership, collisions, test requirements, and release
authority separately.

Stop on a second writer, overlapping file boundary, unresolved design choice,
invented customer claim, unapproved asset, or release mutation beyond the exact
authority granted by the task.
```
